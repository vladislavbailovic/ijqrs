JQ TUI Frontend Tool
====================

`ijqrs` is an interactive TUI frontend for `jq`, written in Rust.


Quick Start
-----------

```console
$ cargo run ./examples/generated.json    # to run
$ cargo install --path .                 # to install
```
